CREATE TABLE PASSWORDHISTORY
(
    ID        BIGINT GENERATED BY DEFAULT AS IDENTITY,
    USER_ID BIGINT NOT NULL      REFERENCES USERTABLE(ID),
    PASSWORD VARCHAR(255) NOT NULL,
    CREATEDAT  TIMESTAMP DEFAULT now()
);